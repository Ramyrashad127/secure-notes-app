global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "web"
    metrics_path: /api/metrics
    bearer_token_file: /etc/prometheus/metrics_token
    static_configs:
      - targets: ["web:3000"]