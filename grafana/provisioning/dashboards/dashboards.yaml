apiVersion: 1

providers:
  - name: "Secure Notes Telemetry"
    orgId: 1
    folder: "Secure Notes"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards